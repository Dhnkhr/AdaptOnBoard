version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
      - "8000:8000"
    env_file:
      - .env
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    restart: unless-stopped
